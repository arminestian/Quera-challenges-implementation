# قاشق و چنگال دور میز 🍽️

**محدودیت زمان:** ۰.۵ ثانیه  
**محدودیت حافظه:** ۶۴ مگابایت

---

دور یک میز گرد، `n` بشقاب قرار دارد. امین می‌خواهد کنار هر بشقاب **دقیقاً یک قاشق و یک چنگال** قرار دهد.

او برای این کار یک رشته از حروف `S` (قاشق) و `F` (چنگال) به طول `2n` انتخاب می‌کند. سپس از یکی از بشقاب‌ها شروع کرده و به ترتیب در جهت ساعتگرد حرکت می‌کند؛ در مرحلهٔ `i`ام (از `1` تا `2n`) اگر حرف `i`ام رشته `S` باشد، یک قاشق کنار بشقاب فعلی قرار می‌دهد، و اگر `F` باشد، یک چنگال کنار بشقاب فعلی قرار می‌دهد. (پس هر بشقاب دقیقاً دو مرحله—دو ورودی رشته—دارد: یکی برای قاشق/چنگال اول و دیگری برای قاشق/چنگال دوم.)

اگر در پایان کار **کنار هر بشقاب یک قاشق و یک چنگال** قرار گرفته باشد، پاسخ `YES` و در غیر این صورت `NO` است.

---

## ورودی
- سطر اول: عدد صحیح و مثبت `n` (تعداد بشقاب‌ها).  
```

1 ≤ n ≤ 100

```
- سطر دوم: یک رشتهٔ طول `2n` شامل حروف `S` و `F`.

> توجه: رشته فقط شامل حروف بزرگ `S` و `F` خواهد بود (حساس به بزرگ/کوچک).

---

## خروجی
در تنها سطر:
- اگر پس از قرار دادن همهٔ اشیاء کنار بشقاب‌ها، **هر بشقاب دقیقاً یک قاشق و یک چنگال** داشته باشد، چاپ کنید:
```

YES

```
- در غیر این صورت چاپ کنید:
```

NO

```

---

## توضیح کوتاه راه‌حل (بدون پیاده‌سازی)
هر بشقاب دو ورودی از رشته را دریافت می‌کند. کافی است برای هر بشقاب شمارش کنیم که از دو ورودی‌اش چندتای `S` و چندتای `F` آمده؛ اگر برای همهٔ بشقاب‌ها شمارش برابر با یک `S` و یک `F` شد، جواب `YES` است، در غیر این صورت `NO`.

---

## نمونه‌ها

### نمونه ۱
**ورودی**
```

2
SFFS

```
**خروجی**
```

YES

```

### نمونه ۲
**ورودی**
```

2
SFSF

```
**خروجی**
```

NO

```

### نمونه ۳
**ورودی**
```

3
SSSSFF

```
**خروجی**
```

NO

```

### نمونه ۴
**ورودی**
```

4
FSFFSFSS

```
**خروجی**
```

YES